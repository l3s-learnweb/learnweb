# If you need to update properties for local development, do that in your local file (copy it and rename to `.env`)
# This properties are only applied for development profile which is active by default, but disabled on production

# Setup the database connection
DATASOURCE_URL=jdbc:mariadb://localhost:3306/learnweb
DATASOURCE_USERNAME=learnweb
DATASOURCE_PASSWORD=

# Decide where you want to locally store files uploaded to Learnweb (primarily needed for Group's resources)
# On Windows the path should look like this: L:\\learnweb\\dev_files
FILE_MANAGER_FOLDER=/tmp/learnweb

# Solr is required for Group's resources navigation, change the URL
SOLR_SERVER_URL=http://localhost:8982/solr/learnweb_dev

# To use Search, you need to configure the integration with InterWeb
INTEGRATION_INTERWEB_URL=https://learnweb.l3s.uni-hannover.de/interweb/api/
INTEGRATION_INTERWEB_APIKEY=
INTEGRATION_INTERWEB_SECRET=

# If you need to work with videos, install FFmpeg on your system and update these paths
FFMPEG_PATH=/usr/bin/ffmpeg
FFPROBE_PATH=/usr/bin/ffprobe
